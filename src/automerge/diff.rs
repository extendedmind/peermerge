use automerge::{Automerge, Prop, Value, ROOT};

use crate::common::entry::Entry;

/// Apply changens to an Automerge document
pub(crate) fn apply_changes(doc: &mut Automerge, changes: Vec<Entry>) {
    // doc.apply_changes();
}
